import 'package:flutter/material.dart';

class TransactionsWithId extends StatelessWidget {
  const TransactionsWithId({
    Key? key,
    required this.id,
  }) : super(key: key);

  final String id;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
